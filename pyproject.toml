[tool.uv.workspace]
members = ["prototype", "src"]

[project]
name = "openmedimgdataval"
version = "0.1.0"
description = "Context-Aware and Efficient Data Valuation for Medical Machine Learning"
readme = "README.md"
requires-python = ">=3.11,<3.12"
dependencies = [
    "ipykernel>=7.1.0",
    "jupyterlab>=4.0.0",
]

[project.optional-dependencies]
dev = [
    "pre-commit>=4.0.0",
    "ruff==0.14.10",
]

[tool.ruff]
line-length = 120
target-version = "py311"

[tool.ruff.lint]
select = [
    "E",    # pycodestyle errors
    "W",    # pycodestyle warnings
    "F",    # pyflakes
    "I",    # isort
    "N",    # pep8-naming
]
ignore = ["E501", "N812", "N803", "N806"]
